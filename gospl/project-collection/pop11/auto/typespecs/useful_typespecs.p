compile_mode :pop11 +strict;

section;

uses isarray:typespec
uses isboolean:typespec     ;;; This should be packed, probably?
uses isclosure:typespec
uses iscomplex:typespec
uses iscompound:typespec
uses isddecimal:typespec
uses isdecimal:typespec
uses isdevice:typespec
uses isdstring:typespec
uses isident:typespec
uses isinteger:typespec     ;;; This should be the same as pint.
uses isintegral:typespec
uses isintvec:typespec
uses iskey:typespec
uses islist:typespec
uses isnumber:typespec
uses ispair:typespec
uses isprocedure:typespec
uses isprocess:typespec
uses isproperty:typespec
uses isratio:typespec
uses isreal:typespec
uses isrecordclass:typespec
uses isref:typespec
uses isshortvec:typespec
uses issimple:typespec
uses isstring:typespec
uses isvector:typespec
uses isvector:typespec
uses isvectorclass:typespec
uses isword:typespec

;;; -- ALSO --

uses flag:typespec

endsection;
