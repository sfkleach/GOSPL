HELP XT_NEW_LABEL                                       Steve Knight, Oct 93
                                                        SIREN Project

xt_new_label( PARENT, [NAME], BINDING1, ..., BINDINGn, [MANAGED] ) -> WIDGET

This procedure creates a Motif label widget.  The arguments are
processed using *XT_NEW_ARGS.

Examples:

In these one-line examples, -my_parent- is a variable bound to a row
column widget:

    xt_new_label( my_parent ) -> w
    xt_new_label( my_parent, 'fred' ) -> w

See also:
    HELP *XT_NEW_ARGS
