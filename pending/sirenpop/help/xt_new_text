HELP XT_NEW_TEXT                                        Steve Knight, Oct 93
                                                        SIREN Project

    xt_new_text( PARENT, [NAME], BINDING1, ..., BINDINGn ) -> WIDGET

This procedure creates a row column widget.  The arguments are processed
using *XT_NEW_ARGS.

Examples:

In these one-line examples, -my_parent- is a variable bound to a
row-column widget.

    xt_new_text( my_parent ) -> w
    xt_new_text( my_parent, 'fred' ) -> w

See also:
    HELP *XT_NEW_ARGS
